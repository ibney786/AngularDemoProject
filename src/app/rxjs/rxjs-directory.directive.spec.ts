import { RxjsDirectoryDirective } from './rxjs-directory.directive';

describe('RxjsDirectoryDirective', () => {
  it('should create an instance', () => {
    const directive = new RxjsDirectoryDirective();
    expect(directive).toBeTruthy();
  });
});
